name: Setup

runs:
  using: composite
  steps:
    - uses: actions/setup-node@v3
      with:
        node-version: 14.x
        cache: npm
    - name: Install dependencies
      run: npm ci --prefer-offline
      shell: bash
      env:
        SKIP_COMPILE: true
